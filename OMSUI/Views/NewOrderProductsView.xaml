<UserControl x:Class="OrderManagerDesktopUI.Views.NewOrderProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OrderManagerDesktopUI.Views" 
             xmlns:vm="clr-namespace:OrderManagerDesktopUI.ViewModels"
             mc:Ignorable="d" 
             d:DesignHeight="425" d:DesignWidth="510"
             Background="AliceBlue">
    <Grid>
        <StackPanel DataContext="{Binding RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=local:NewOrderView}}">
            <DataGrid Grid.Row="1"
                  AutoGenerateColumns="False"
                  VerticalAlignment="Top"
                  ItemsSource="{Binding DataContext.Products, UpdateSourceTrigger=PropertyChanged}"
                  SelectedItem="{Binding DataContext.SelectedProduct, UpdateSourceTrigger=PropertyChanged}">
                <DataGrid.Columns>
                    <!-- Product ID -->
                    <DataGridTextColumn Header="No."
                    Binding="{Binding ProductId, UpdateSourceTrigger=PropertyChanged}"
                    Width="50"/>

                    <!-- Product Name -->
                    <DataGridTextColumn Header="Magac"
                    Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged}"
                    Width="200"/>

                    <!-- Product Price -->
                    <DataGridTextColumn Header="Qiimo"
                    Binding="{Binding Price, UpdateSourceTrigger=PropertyChanged}"
                    Width="200"/>

                    <!-- Add Button -->
                    <DataGridTemplateColumn Width="*">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <Button Content="Ku dar"
                            Command="{Binding Path=DataContext.AddToOrderCommand, RelativeSource={RelativeSource Mode=FindAncestor, AncestorType=local:NewOrderView}}"
                            CommandParameter="{Binding}"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
            <Label Content="{Binding DataContext.SelectedProduct.Name}"
               FontSize="24"/>
        </StackPanel>
    </Grid>
</UserControl>
