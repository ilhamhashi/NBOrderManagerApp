<UserControl x:Class="OrderManagerDesktopUI.Views.ProductsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:OrderManagerDesktopUI.Views" 
             xmlns:Icon="http://metro.mahapps.com/winfx/xaml/iconpacks"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="70*"/>
            <ColumnDefinition Width="30*"/>
        </Grid.ColumnDefinitions>       

        <Border Grid.Column="0"
                Style="{StaticResource whiteBorder}" 
                Margin="15 0 7 0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="10*" />
                    <RowDefinition Height="10*" />
                    <RowDefinition Height="80*" />
                </Grid.RowDefinitions>

                <!-- Search field that filters product list -->
                <Grid Background="White" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"  >
                    <!-- overlay with hint text -->
                    <TextBlock Margin="5,2" FontSize="14" Text="Search product" 
                       Visibility="{Binding ElementName=txtSearchProducts, Path=Text.IsEmpty, Converter={StaticResource MyBoolToVisibilityConverter}}" 
                       IsHitTestVisible="False" />
                    <!-- enter term here -->
                    <TextBox Text="{Binding SearchTerm, UpdateSourceTrigger=PropertyChanged}" FontSize="14"
                     TextWrapping="Wrap" AcceptsReturn="True" Name="txtSearchProducts" Background="Transparent" />
                </Grid>

                <TextBlock Grid.Row="1" 
                           Text="Products"
                           HorizontalAlignment="Left"
                           VerticalAlignment="Center"
                           FontSize="20"
                           Margin="10 0 0 20"/>

                <!-- List of products -->
                <ScrollViewer VerticalScrollBarVisibility="auto"
                              Grid.Row="2">
                    <DataGrid RowStyle="{DynamicResource DataGridRowStyle1}" 
                          ColumnHeaderStyle="{DynamicResource DataGridColumnHeaderStyle1}" 
                          CellStyle="{DynamicResource DataGridCellStyle1}" 
                          Style="{DynamicResource DataGridStyle1}"
                          ItemsSource="{Binding ProductsCollectionView, UpdateSourceTrigger=PropertyChanged}"
                          SelectedItem="{Binding SelectedProduct, UpdateSourceTrigger=PropertyChanged}"
                          HorizontalAlignment="Stretch">
                        <DataGrid.Columns>
                            <DataGridTextColumn Header="#" IsReadOnly="True" Width="auto" 
                                Binding="{Binding Id, UpdateSourceTrigger=PropertyChanged}"/>
                            <DataGridTextColumn Header="Name" IsReadOnly="True" Width="*"
                                Binding="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>
                            <DataGridTextColumn Header="Description" IsReadOnly="True" Width="*"
                                Binding="{Binding Description, UpdateSourceTrigger=PropertyChanged}"/>
                            <DataGridTextColumn Header="Price" IsReadOnly="True" Width="*"
                                Binding="{Binding Price, StringFormat={}{0:C2}, UpdateSourceTrigger=PropertyChanged}"/>
                            <DataGridTemplateColumn Header="Size Options" Width="auto">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ComboBox ItemsSource="{Binding SizeOptions}"
                                              Style="{DynamicResource ComboBox}"
                                              SelectedIndex="0">
                                            <ComboBox.ItemTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />
                                                </DataTemplate>
                                            </ComboBox.ItemTemplate>
                                        </ComboBox>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Taste Options" Width="auto">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <ComboBox ItemsSource="{Binding TasteOptions}"
                                              Style="{DynamicResource ComboBox}"
                                              SelectedIndex="0">
                                            <ComboBox.ItemTemplate>
                                                <DataTemplate>
                                                    <TextBlock Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" />
                                                </DataTemplate>
                                            </ComboBox.ItemTemplate>
                                        </ComboBox>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                            <DataGridTemplateColumn Header="Delete" IsReadOnly="True" Width="auto">
                                <DataGridTemplateColumn.CellTemplate>
                                    <DataTemplate>
                                        <Button Background="Transparent" BorderThickness="0"
                                                Style="{DynamicResource gridAddButton}"
                                                Command="{Binding DataContext.DeleteProductCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                                CommandParameter="{Binding}">
                                            <Icon:PackIconMaterial Kind="Delete" Foreground="#222528" />
                                        </Button>
                                    </DataTemplate>
                                </DataGridTemplateColumn.CellTemplate>
                            </DataGridTemplateColumn>
                        </DataGrid.Columns>
                    </DataGrid>
                </ScrollViewer>
            </Grid>
        </Border>

        <Border Grid.Column="1" Style="{StaticResource whiteBorder}" Margin="7 0 0 0">
            <Grid>
                <ContentControl Grid.Row="1" 
                                Content="{Binding Navigation.CurrentNestedView}"
                                Margin="15"/>
            </Grid>
        </Border>

    </Grid>
</UserControl>
